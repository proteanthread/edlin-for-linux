# Project Compilation Rules

- **C Standards**: All `.c` files must be compiled and compliant with no standard older than C17 (e.g., `-std=c17`).
- **C++ Standards**: All `.cpp` files must be compiled and compliant with no standard older than C++17 (e.g., `-std=c++17`).
